<apex:page standardController="Account" >
  
  <apex:pageBlock >  
    <apex:pageBlockTable value="{!Account.Contacts}" var="Contact"> 
      <apex:column value="{!Contact.name}"/>
      <apex:column value="{!Contact.OutletName__c}"/>
      <apex:column value="{!Contact.Phone}"/>
    </apex:pageBlockTable>
  </apex:pageBlock>

</apex:page>